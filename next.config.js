/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Usa esta forma para desactivar LightningCSS
    disableExperimentalCss: true
  }
};

module.exports = nextConfig;

